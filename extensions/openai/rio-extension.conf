name = openai
kind = provider
provider-api = 2
version = 1.1.0
author = jlsksr
description = OpenAI-compatible agent provider — hosted ChatGPT (an OpenAI API key), or a local OpenAI-compatible server (Ollama, llama-server) via its base URL
entry = openai.tcl
files = openai.tcl inference.tcl api-face.tcl
