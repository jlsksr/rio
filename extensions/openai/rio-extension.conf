name = openai
kind = provider
provider-api = 4
version = 1.2.0
author = jlsksr
description = OpenAI-compatible agent provider — hosted ChatGPT, or any server speaking the same protocol (Ollama, llama.cpp, vLLM, LM Studio): set its URL, model and request options in Extensions ▸ OpenAI-compatible…
entry = openai.tcl
files = openai.tcl inference.tcl api-face.tcl
